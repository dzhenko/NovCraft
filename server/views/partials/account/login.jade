.navbar-right
    form.navbar-form
        .form-group
            input.form-control(type="text", placeholder="Username", gn-model="user.username")
            input.form-control(type="password", placeholder="Password", gn-model="user.username")
        button.btn.btn-primary(ng-click="login(user)") Login
